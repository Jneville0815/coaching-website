# coaching-website

Jimmy Neville's coaching site, live at https://jimmyneville.com.

Plain HTML and CSS with no JavaScript and no build step. GitHub Pages serves it
from `main`, so a push to `main` publishes it. `CNAME` holds the custom domain.

## Preview locally

```sh
python3 -m http.server 8000
```

Then open http://localhost:8000.
